# Core data stack
nba_api>=1.4.1        # official-ish community wrapper around stats.nba.com endpoints
pandas>=2.0          # dataframes, temporal joins, rolling windows
numpy>=1.24          # numerics (possession formula, vectorized features)
pyarrow>=14.0        # parquet engine used for the data/raw cache

# Modeling & evaluation
scikit-learn>=1.3    # LogisticRegression, metrics, pipelines, scaling
joblib>=1.3          # sérialisation du pipeline entraîné (models/logreg.joblib)
matplotlib>=3.7      # confusion matrix / ROC figures saved to reports/

# Interface & exploration
streamlit>=1.30      # interface visuelle interactive (app.py)
jupyter>=1.0         # notebooks/exploration.ipynb
